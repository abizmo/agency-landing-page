---
import Sunnyside from "../assets/logo.svg";

interface Props {
  size?: "base" | "large";
}

const { size = "base" } = Astro.props;

const height = size === "base" ? 24 : 32;
const width = 124 * (height / 24);
---

<Sunnyside width={width} height={height} fill="currentColor" />
